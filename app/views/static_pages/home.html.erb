<% if logged_in? %>
    <div class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
  </div>
<% else %>
<div class="bPhotos"></div>
<%= render 'sessions/new' %>
<% end %>